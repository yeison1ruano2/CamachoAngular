import { RegistrarUsuario } from './registrar-usuario.model';

describe('RegistrarUsuario', () => {
  it('should create an instance', () => {
    expect(new RegistrarUsuario()).toBeTruthy();
  });
});
